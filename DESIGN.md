# 慧排座设计语言

## 总体方向

慧排座是面向老师的排座管理后台，不是营销站点，也不是花哨的消费级应用。整体视觉要体现四个关键词：

- 清晰。
- 克制。
- 高效。
- 可信。

本项目采用 **Airtable 浅色工作台风格** 作为基础气质，并结合慧排座的业务特点做定制化收口：

- 以白底和浅灰底为主，不使用深色主题作为默认方案。
- 以表格、列表、卡片和右侧信息栏组织内容。
- 强化结构化数据的可读性，弱化装饰性视觉。
- 使用少量品牌蓝作为主强调色，状态色保持明确但不刺眼。
- 页面应看起来像老师在用的一套稳定工作台，而不是实验性产品。

## 视觉原则

1. **表格感强，但不做 Excel 复制品**
   - 班级、学生、规则、成绩、方案列表可以有明显表格感。
   - 需要保留足够留白，避免像传统报表一样拥挤。
   - 重点不是“多”，而是“看得清、找得到、改得快”。

2. **浅色优先**
   - 默认背景使用白色、极浅灰和少量浅蓝。
   - 不使用大面积深色背景。
   - 深色只用于少量文本强调，不作为全局视觉基调。

3. **层级明确**
   - 页面标题、模块标题、表格标题、辅助说明要有稳定层级。
   - 视觉重点只给真正需要老师立刻判断的内容。
   - 复杂页面优先保证信息路径清晰，而不是堆叠视觉效果。

4. **少装饰**
   - 少用强阴影、重渐变、花哨背景和夸张动效。
   - 优先使用细边框、柔和底色、适度圆角和轻量状态提示。

5. **操作可预期**
   - 按钮、标签、提示、状态文案都要稳定一致。
   - 视觉变化必须对应明确功能，不做纯装饰性变化。

## 颜色建议

### 基础色

- 页面背景：`#f7f8fa`
- 内容卡片背景：`#ffffff`
- 分隔线：`#e6e8eb`
- 主文本：`#1f2329`
- 次文本：`#5f6b7a`
- 弱文本：`#8a94a6`

### 品牌强调色

- 主按钮、选中态、关键链接：使用品牌蓝系。
- 推荐主色方向：清爽、可信、不过饱和。
- 不要用过亮荧光蓝，也不要用偏紫的默认 UI 蓝。

### 状态色

- 成功：浅绿到中绿，提示明确但不刺眼。
- 警告：浅橙到中橙，适合规则冲突、保存提示。
- 失败：浅红到中红，适合加载失败或操作失败。
- 信息：浅蓝系，适合说明、引导、当前选中态。

### 使用原则

- 一个页面最多只有一个最强强调色主导注意力。
- 状态色只用于真正有业务意义的地方。
- 不要让颜色承担过多层级任务，层级更多依赖排版和布局。

## 字体与排版

- 中文界面优先使用系统无衬线字体体系，保持稳定、清晰、易读。
- 标题字号要比正文有明显区分，但不要夸张放大。
- 列表、表格和详情页中的数字要统一对齐，保证快速扫读。
- 说明性文字保持简短，避免长段落堆叠。

## 布局方式

### 列表页

- 以表格为主。
- 搜索、筛选、操作按钮放在列表上方。
- 重要操作按钮固定在视觉右侧或标题旁边，避免分散。
- 空状态和失败状态要清楚区分，不要只显示空白。

### 详情页

- 采用“主工作区 + 侧信息栏”结构。
- 主工作区承载座位表、拖拽、批量操作等核心任务。
- 侧栏承载评分明细、方案差异、未安排学生、规则解释等辅助信息。
- 详情页既要让老师看懂，也要让老师能直接操作，不能只做展示页。

### 方案详情页

- 座位表区域要足够大，避免缩成小图。
- 外圈排号和列号要清晰可见，帮助老师定位。
- 座位状态通过浅色底、边框和标签表达，不要靠复杂图标堆叠。
- 评分明细和方案差异要是“能扫读的摘要”，不是技术日志。

## 组件风格

### 表格

- 表头简洁，边框轻。
- 行高适中，保证高密度数据下仍可阅读。
- 优先支持横向扫读和快速定位。
- 关键列可以适度加粗或强调，但不要滥用高亮。

### 卡片

- 卡片使用白底、细边框、轻圆角。
- 卡片之间留出足够间距，体现分区。
- 卡片内部信息遵循“标题、摘要、动作”的固定顺序。

### 按钮

- 主按钮只保留一个最重要的动作。
- 次级按钮保持克制，不要同时出现太多强颜色按钮。
- 危险操作要明确，但不要夸张。

### 标签与状态

- 标签用于分类、状态、性别、方案状态等。
- 文本要短，颜色要轻，避免彩虹式标签。
- 状态标签应让老师一眼知道“现在是什么状态”。

### 提示与空状态

- 空状态要告诉老师下一步怎么做。
- 失败状态要告诉老师是否可以重试。
- 提示文案尽量业务化，不暴露技术术语。

## 业务页面建议

### 班级管理

- 适合表格感较强的浅色列表。
- 行内状态、操作按钮、筛选区要保持整齐。
- 学年、学期、学段、班级科目等字段优先以清晰标签呈现。

### 学生管理

- 适合高密度表格。
- 学号、班级、成绩等级、视力、纪律等字段要方便扫读。
- 导入、筛选、编辑流程都要尽量短。

### 教室布局

- 适合网格化编辑。
- 需要强调座位、过道、不可用位置之间的差异。
- 不宜做成纯表单界面，应该有明显的布局感。

### 排座规则

- 规则列表保持清晰、克制、易比较。
- 启用状态、权重、规则说明要一眼能分辨。
- 如果有高级配置，应该收纳到二级区域，不抢主视觉。

### 座位方案

- 这是本项目最重要的工作台页面。
- 重点是“看懂方案、看懂差异、快速微调、稳定保存”。
- 座位区要像工作区，右侧信息栏要像操作助手。

## 不要做的事

- 不要默认做深色主题。
- 不要把页面做得像宣传站。
- 不要使用过多大渐变、大阴影和大面积装饰图形。
- 不要让表格变成杂乱的报表。
- 不要让老师看到太多技术字段、内部 ID 或调试式文案。
- 不要为了“高级感”牺牲清晰度。

## 交付标准

一个符合本设计语言的页面，应该满足下面几点：

1. 老师在 3 秒内知道这页是干什么的。
2. 老师在 10 秒内能找到主要操作入口。
3. 老师能扫读表格、卡片和提示，不需要猜测含义。
4. 颜色能辅助理解，但不会抢走内容本身的注意力。
5. 整体看起来浅色、干净、稳定、适合学校后台长期使用。

