<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.seating.mapper.SeatSubjectMapper">

    <resultMap type="SeatSubject" id="SeatSubjectResult">
        <result property="subjectId" column="subject_id" />
        <result property="schoolStage" column="school_stage" />
        <result property="gradeCode" column="grade_code" />
        <result property="subjectCode" column="subject_code" />
        <result property="subjectName" column="subject_name" />
        <result property="sortOrder" column="sort_order" />
        <result property="isRequired" column="is_required" />
        <result property="status" column="status" />
        <result property="remark" column="remark" />
    </resultMap>

    <select id="selectSeatSubjectList" parameterType="SeatSubject" resultMap="SeatSubjectResult">
        select subject_id, school_stage, grade_code, subject_code, subject_name, sort_order, is_required, status, remark
        from seat_subject
        <where>
            <if test="schoolStage != null and schoolStage != ''"> and school_stage = #{schoolStage}</if>
            <if test="gradeCode != null and gradeCode != ''"> and grade_code = #{gradeCode}</if>
            <if test="status != null and status != ''"> and status = #{status}</if>
        </where>
        order by sort_order, subject_id
    </select>
</mapper>
