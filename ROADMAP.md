## 2026-06-27 ?????????????

- ?????????????????????????
- ???????????????????????
- ??? `npm run build:prod` ???

## 2026-06-27 ???????????

- ??????????????????????????????????????????
- ??????? `change` ?????? ID???????????????
- ??? `node --test` ? `npm run build:prod` ???
- ???????????????????????

## 2026-06-27 前端构建与浏览器可用性复核

已确认状态：

- 2026-06-27 已重新执行前端生产构建，`yarn build:prod` 当前可通过。
- 2026-06-27 已重新接通内置浏览器实例，当前会话可以继续做座位方案详情页真实页面回归。
- 2026-06-27 已进入座位方案详情页剩余交互和导出链路的继续验证阶段。

# ROADMAP.md

## 项目目标

交付「慧排座」PC Web 后台管理系统，服务老师在电脑端完成班级管理、学生维护、教室布局配置、排座规则配置、一键生成座位表、人工拖拽微调、历史方案保存和导出。

日期：2026-06-27

当前阶段：阶段 8「MVP 收尾」已完成，项目已进入阶段 9「体验增强」。

已确认状态：

- MVP 主闭环已经完成，并通过真实浏览器回归验证。
- 阶段 9 的 `P0` 已完成。
- 阶段 9 的 `P1` 已完成第一轮实现，并已启动收口。
- 2026-06-27 内置浏览器实例已恢复，阶段 9 P1 的真实页面回归继续进行，方案详情页保存后刷新回显与恢复回显均已验证。
- 2026-06-27 已用真实浏览器复核批量解锁路径：在已保存锁定基线上，批量解锁后保存按钮可点击，随后恢复原基线后保存按钮再次禁用。
- 2026-06-27 已完成座位方案详情页批量解锁路径的代码级修复，并通过本地单测与 SFC 解析验证；真实浏览器复核待后续恢复可用实例后继续。
- 2026-06-27 已将详情页 `dirty` 切为自动计算，减少批量解锁后按钮状态滞后的风险；全量构建当前仍受既有 Vite 配置问题阻断。
- 阶段 9 的 `P2` 仍处于规划中，尚未进入正式开发收口。

当前结论：

- 项目当前重点不再是补核心业务能力，而是继续降低老师操作成本、提升方案可理解性，并补齐演示与交付基线。

已完成：

- 确认后端使用 `RuoYi-Vue-springboot3`。
- 确认前端使用 `RuoYi-Vue3-master`。
- 验证后端 `8310` 和前端 `8222` 联通。
- 完成排座业务模块设计文档。
- 完成排座业务数据库设计文档。
- 新增排座业务 SQL 草案：`RuoYi-Vue-springboot3/sql/seating_20260615.sql`。
- 创建并接入 `ruoyi-seating` Maven 模块。
- 后端执行 `mvn -DskipTests compile` 通过。
- 已导入排座业务表并使用若依代码生成器生成基础 CRUD。
- 所有生成代码已确认可以正常运行。
- 班级管理页面已完成第一轮业务化整理，老师 ID、部门 ID 和删除标志不再由前端手填，后端按当前登录用户自动写入。
- 排座 domain、mapper、service 和 Mapper XML 已统一整理到 `ruoyi-seating` 的 `com.ruoyi.seating` 与 `mapper/seating`。
- 排座 Controller 已统一整理到 `ruoyi-admin` 的 `com.ruoyi.web.controller.seating`。
- 后端接口路径和权限标识已统一为 `/seating/*` 和 `seating:*`。
- 前端 API 和页面已统一整理到 `src/api/seating` 和 `src/views/seating`。
- 已新增并导入菜单权限初始化脚本 `RuoYi-Vue-springboot3/sql/seating_menu_20260615.sql`。
- 已验证 `智能排座` 一级菜单、权限和 5 个基础页面可正常访问。
- 班级、学生、教室、规则、方案的新增、查询、修改和逻辑删除已完成端到端联调。
- 5 个基础模块查询已统一过滤 `del_flag=0`，删除已改为 `del_flag=2`。
- 班级导出路径已修正为 `/seating/class/export`。
- 学生导入弹窗组件注册问题已修复，导入模板接口已验证可返回 Excel 文件。
- 学生 Excel 成功导入、同学号覆盖更新和失败回滚已完成接口级验证。
- 后端执行 `mvn clean compile -DskipTests` 通过，前端执行 `npm run build:prod` 通过。
- 已新增 `POST /seating/plan/generate` 智能生成接口。
- 已补充第一版排座引擎输入模型、硬规则冲突提示、固定随机种子和随机交换优化。
- 已支持生成结果保存到 `seat_plan`、`seat_assignment` 和 `seat_plan_score`。
- 前端座位方案页面已新增“一键生成”入口。
- 排座规则新增和修改弹窗已补充硬规则、软规则说明。
- 已使用真实登录态完成 `POST /seating/plan/generate` 端到端运行验收。
- 已验证生成结果可写入 `seat_plan`、`seat_assignment` 和 `seat_plan_score`。
- 已验证硬规则冲突会返回明确错误，且冲突场景不会创建方案记录。
- 已验证同一输入和固定随机种子可复现相同座位分配结果。
- 座位方案列表已新增详情入口，生成成功后可直接进入详情页查看座位表。
- 座位方案详情页已支持按教室布局展示座位、学生分配和评分明细。
- 后端执行 `mvn -DskipTests compile` 通过，前端执行 `npm run build:prod` 通过。
- 座位方案详情页已支持拖拽换座、锁定/解锁座位、空座编辑、未安排学生回填和保存人工微调。
- 人工微调保存后已支持重新计算总分、评分明细和硬规则冲突提示。
- 已支持将草稿或归档方案确认为当前启用方案，同班级仅保留一个启用方案。
- 已支持复制历史方案为新草稿，并支持归档方案恢复启用。
- 座位方案导出已支持 Excel 二维座位表、PNG 图片和浏览器打印保存 PDF。
- 座位方案新增、修改和智能生成已支持明确命名，复制方案时可指定副本名称。
- Excel、PNG 和 PDF 导出名称已统一使用“方案名称 + 当前视角 + 座位表”。
- 用户已确认成绩管理真实登录态联调和 Excel、PNG、PDF 导出内容校验完成。
- 管理后台首页已移除若依默认介绍、官网链接、QQ群、捐赠和更新日志内容，替换为智能排座业务工作台。
- 系统名称已确定为「慧排座」，前端标题、登录页、侧边栏 Logo、页脚版权和项目文档已同步更新。
- 「慧排座」Logo 和 SVG favicon 已接入前端，侧边栏 Logo 与浏览器图标已切换到新品牌资源。

## MVP 范围

第一版包含：

- 老师登录。
- 班级管理。
- 学生管理。
- Excel 导入学生。
- 教室座位布局配置。
- 学生排座属性维护。
- 基础排座规则配置。
- 一键智能生成座位表。
- 座位表拖拽微调。
- 锁定座位。
- 保存历史座位方案。
- 导出 Excel / PDF / 图片。

第一版不包含：

- 微信小程序。
- 学生端。
- 家长端。
- 多学校多租户。
- 复杂 AI 对话式排座。
- 复杂审批流。

## 阶段 1：基础工程和模块边界

目标：让若依基础工程稳定运行，并建立排座模块边界。

状态：基本完成。

已完成：

- 前后端源码下载到本地。
- 后端和前端本地服务验证联通。
- 新增 `ruoyi-seating` Maven 模块。
- 根 `pom.xml` 接入 `ruoyi-seating`。
- `ruoyi-admin` 依赖 `ruoyi-seating`。
- 创建排座模块基础包结构。

验收标准：

- `mvn -DskipTests compile` 成功。
- Maven reactor 能识别 `ruoyi-seating`。
- `ruoyi-admin` 依赖 `ruoyi-seating` 后仍能编译。
- 根目录存在 `AGENTS.md` 和 `ROADMAP.md`。

## 阶段 2：数据库导入和代码生成

目标：完成排座基础数据表落库，并生成基础 CRUD。

状态：已完成。

已完成：

- 导入 `RuoYi-Vue-springboot3/sql/seating_20260615.sql`。
- 使用若依代码生成器导入 `seat_` 业务表。
- 生成班级、学生、教室、座位、规则、方案、分配结果等基础代码。
- 生成代码已确认可以正常运行。
- 生成代码的 domain、mapper、service 和 Mapper XML 已整理到 `ruoyi-seating`。
- Controller 已整理到 `ruoyi-admin` 的 `com.ruoyi.web.controller.seating`。
- 前端 API 和页面已整理到 `src/api/seating` 和 `src/views/seating`。
- 接口路径和权限标识已统一为 `/seating/*` 和 `seating:*`。

验收标准：

- 所有 `seat_` 表可被代码生成器识别。
- 基础 CRUD 接口可启动访问。
- 后端编译通过。
- 不把排座业务代码放入 `ruoyi-system`。

## 阶段 3：菜单、权限和基础页面

目标：让老师能从后台菜单进入排座基础管理页面。

状态：已完成。

已完成：

- 班级管理页面已移除不应手填的负责老师 ID、所属部门 ID、删除标志。
- 班级新增时后端已按当前登录用户自动写入负责老师和所属部门。
- 班级状态已改为启用/停用字典控件。
- 学生新增和修改表单已改为选择启用班级，不再手工输入班级 ID。
- 学生管理查询、列表和表单已完成第一轮业务化：性别、视力、成绩、纪律和状态使用选择控件，身高和排序号使用数字控件，列表显示班级名称和中文枚举。
- 已准备 `智能排座` 一级菜单及 5 个基础页面的菜单权限 SQL。
- 菜单权限 SQL 已导入数据库。
- `智能排座` 一级菜单、权限和 5 个基础页面已验证可正常访问。
- 班级、学生、教室、规则、方案基础 CRUD 已完成端到端联调。
- 5 个基础模块已统一使用逻辑删除并过滤已删除数据。
- 前端 `/dev-api` 代理访问排座接口正常。

验收标准：

- 登录后可看到智能排座菜单。
- 超级管理员可访问全部排座页面。
- 普通角色按权限控制按钮显示。
- 基础 CRUD 前后端联通。

## 阶段 4：学生导入和教室布局

目标：完成排座前的核心数据准备能力。

状态：已完成。

已完成：

- 学生 Excel 导入模板和导入入口。
- 学生导入弹窗组件注册已修复，模板下载入口恢复。
- 学生导入字段校验、错误行提示和事务回滚。
- 支持按学号更新班级内已有学生。
- 学生 Excel 成功导入、同学号覆盖更新和失败回滚已完成运行验证。
- 教室按行列初始化座位位置。
- 教室座位初始化已完成接口级验证，重复初始化后座位数量保持正确。
- 教室新增接口已返回生成后的 `classroomId`，支持前端新增后继续保存座位布局。
- 已新增按教室覆盖保存座位布局接口，并补充座位接口的数据权限校验。
- 教室布局页面已改为网格化编辑，支持点击切换座位、过道和不可用。
- 阶段 4 数据准备能力端到端验收已通过。

验收标准：

- 老师可以批量导入学生。
- 导入失败时能看到明确错误。
- 老师可以创建一个可用于排座的教室布局。
- 座位位置数据能正确保存到 `seat_position`。

## 阶段 5：第一版排座算法

目标：实现可解释的规则评分加随机交换优化。

状态：基本完成。

已完成：

- 扩展算法输入模型，支持学生、座位、规则、学生关系、固定随机种子和优化次数。
- 扩展算法输出模型，支持座位分配、总评分、评分明细和硬规则冲突提示。
- 实现第一版硬规则校验：
  - 特殊需求包含“前排”或规则编码为 `FRONT_ROW` / `MUST_FRONT_ROW` / `STUDENT_FRONT_ROW` 的学生需坐前排。
  - 学生关系 `NOT_DESKMATE` 不允许同桌。
  - 学生关系 `NOT_ADJACENT` 不允许相邻。
  - 仅使用 `seat_position` 中普通且可用的座位，过道和不可用座位不参与自动分配。
- 实现第一版软规则评分：
  - 近视学生尽量靠前。
  - 身高较高学生尽量靠后。
  - 男女搭配尽量均衡。
  - 成绩强弱尽量均衡。
  - 纪律较差学生尽量分散。
  - 学生关系 `PREFER_DESKMATE` 作为软约束评分。
- 实现固定随机种子的随机交换优化。
- 新增 `POST /seating/plan/generate` 生成接口。
- 前端座位方案页面新增“一键生成”入口。
- 生成结果可保存到 `seat_plan`、`seat_assignment` 和 `seat_plan_score`。
- 真实登录态下生成接口端到端运行验收通过，4 名学生、6 个座位可生成完整方案。
- 生成方案可查询到 4 条座位分配和 9 条评分明细。
- 硬规则冲突场景返回 `硬规则冲突` 提示，且未创建方案记录。
- 同一输入使用固定随机种子生成两次，座位分配结果一致。

待办：

- 补充历史方案输入，用于“尽量减少频繁换座”评分。
- 补充锁定座位输入，用于再次生成时保持锁定座位不变。
- 在页面查看评分明细。

验收标准：

- 可对一个班级生成完整座位方案。
- 硬规则冲突有明确提示。
- 软规则评分可查看。
- 同一输入可通过固定随机种子复现结果，便于调试。

## 阶段 6：座位表编辑和人工微调

目标：让老师能直观看到座位表，并进行拖拽调整。

状态：基本完成。

已完成：

- 实现座位方案详情页。
- 座位方案列表新增「查看」入口。
- 一键生成成功后自动进入座位方案详情页。
- 按教室布局渲染座位网格，显示讲台、座位、空座、过道和不可用座位。
- 已在详情页展示分配学生和评分明细。
- 支持拖拽已分配学生到空座位，或与其他学生交换座位。
- 支持锁定和解锁单个学生座位，锁定座位禁止拖拽调整。
- 支持将已分配学生移出座位形成空座，并从未安排学生列表拖回空座。
- 支持保存人工微调后的 `seat_assignment`，保存后分配来源更新为 `MANUAL`。
- 支持保存后刷新方案总分、评分明细和硬规则冲突提示。

待办：

- 使用真实登录态补充空座编辑、未安排学生拖回和保存回显的页面联调记录。
- 补充锁定座位参与再次智能生成时保持不变的算法输入和接口联调。

验收标准：

- 座位表布局与教室配置一致。
- 拖拽后能保存并刷新保持一致。
- 锁定座位在再次生成时保持不变。
- 页面在常见 PC 分辨率下可用。

## 阶段 7：导出和历史方案

目标：支持老师保存、对比和导出座位结果。

状态：基本完成。

已完成：

- 支持将草稿或归档座位方案确认为当前启用方案。
- 支持复制已有座位方案为新的草稿方案。
- 支持归档方案恢复启用，恢复后同班级原启用方案自动归档。
- 支持从方案详情页导出 Excel 二维座位表。
- 支持从方案详情页导出 PNG 图片。
- 支持从方案详情页打开打印页并保存为 PDF。
- 支持新增、修改和智能生成方案时指定方案名称。
- 支持复制历史方案时指定新方案名称。
- Excel、PNG 和 PDF 导出名称统一包含方案名称和当前视角。
- 用户已确认真实登录态下 Excel、PNG 和 PDF 导出内容校验完成。

待办：

- 结合演示场景梳理历史方案筛选、复制、恢复的页面体验。
- 使用真实登录态补做复制方案命名交互验证。

验收标准：

- 老师可以查看历史方案。
- 老师可以恢复或复制历史方案。
- 导出内容包含班级、时间、座位表、必要备注。

## 阶段 8：MVP 收尾

目标：完成第一版可演示闭环。

状态：已完成。

已完成：

- 已完成从登录、班级与学生维护、教室布局、规则配置、智能生成、人工微调、确认启用到导出的完整闭环验收。
- 已完成成绩管理、学生导入、排座规则和导出功能的真实操作回归。
- 已完成菜单、权限、初始化 SQL、项目说明和老师使用流程的基础收口。
- 后端 `mvn -DskipTests compile` 与前端 `npm run build:prod` 均已通过。

已知后续项：

- 演示数据、部署说明和非核心异常提示将在后续迭代中持续完善，不阻塞 MVP 验收。

验收标准：

- 从登录到生成、微调、保存、导出形成完整闭环。
- 新环境按 SQL 和说明可初始化。
- 后端编译通过。
- 前端可正常启动并访问核心页面。

## 阶段 9：体验增强

目标：在不扩张 MVP 业务边界的前提下，降低老师完成排座任务的操作成本，提高方案结果的可理解性，并形成稳定的演示与交付基线。

状态：`P0 已完成，P1 第一轮已完成且已启动收口，P2 待规划实施`。

优先级：

1. `P1` 收口：继续统一批量操作、撤销／重做、保存调整等高频交互的反馈文案、禁用条件和真实数据回归。
2. `P1` 收口：继续提升评分明细、规则解释、冲突提示的教师可读性，帮助老师快速判断是否需要微调。
3. `交付基线`：补齐演示数据、部署说明和稳定回归清单，形成后续演示、验收和继续开发的统一基线。
4. `P2` 规划：在 `P1` 稳定后，再推进班级模板、规则模板、教室布局模板和新学期复制能力。

P0 已完成：

- 班级、学生、教室布局、排座规则、座位方案、考试列表已补齐首次无数据、筛选无结果和加载失败三类空状态。
- 座位方案详情页已补齐加载失败提示、重新加载和返回列表入口。
- 学生与成绩导入已补齐业务失败、网络失败和部分错误反馈，并保留导入上下文。
- 生成、保存调整、确认方案、导出失败场景已补充状态保持验证，失败后不丢当前输入和未保存座位状态。

P1 第一轮已完成：

- 座位方案详情已支持撤销／重做。
- 已支持批量选择座位、批量锁定／解锁。
- 已支持未安排学生按姓名或学号快速筛选。
- 已支持同班历史方案差异对比，显示学生座位变化。
- 已将同班历史方案差异文案改为“原 第x排第y列 → 现 第x排第y列”，与外圈排号／列号展示保持一致。
- 已将同班历史方案差异区改成更紧凑的两列对照行展示，并补齐历史方案下拉的方案名和 ID，同时收短差异摘要，便于老师快速扫读并准确区分重复编号。
- 硬规则冲突提示已补充当前座位位置。
- 已修复座位编辑工具栏在常见桌面宽度下的布局溢出问题。
- 座位方案详情高频工具栏动作已统一第一轮禁用条件与反馈文案，并补充前端动作状态单测。
- 座位方案详情评分明细与冲突提示已完成第一轮老师可读性收口，说明列不再直接暴露技术键名。
- 座位方案详情页 Excel 导出已补齐外圈排号／列号，Excel／PNG／PDF 三类导出口径保持一致。

P1 待收口：

- 补一轮基于真实业务数据的高频操作回归，确认撤销／重做、批量锁定／解锁、保存、确认、导出在连续操作下的反馈是否符合老师预期。
- 根据老师真实使用反馈继续微调说明句与冲突提示，而不是继续堆技术字段。
当前环境状态：

- 2026-06-27 内置浏览器实例已恢复，可继续做真实页面回归。
- 阶段 9 P1 的保存与恢复回显已验证，不再受浏览器实例缺失阻塞。
- 阶段 9 P1 的教师视角／学生视角切换在保存后的方案详情页中也已验证正常。

P2 规划方向：

- 班级模板。
- 排座规则模板。
- 教室布局模板。
- 新学期复制创建能力。

范围约束：

- 继续以 PC Web 管理端为主，不引入小程序、家长端、多租户和 AI 对话式排座。
- 复用能力仅服务已有业务配置复制，不扩展为复杂流程系统。

## 当前优先级

下一步优先顺序：

1. 完成阶段 9 `P1` 收口。
2. 补齐演示数据、部署说明和稳定回归清单。
3. 在 `P1` 稳定后，再进入阶段 9 `P2` 模板复用与新学期复制能力。

当前风险：

- `ROADMAP.md` 若不及时同步，会继续落后于 `docs/project-progress.md` 的实际进度。
- 阶段 9 已实现内容较多，如不尽快统一验收口径，后续容易出现“功能已做、状态不清”的问题。
- 演示数据、部署说明和回归清单若长期缺位，会影响后续演示质量、交付效率和维护成本。

## 2026-06-18 座位方案命名收口记录

已完成：

- 座位方案新增、修改和智能生成表单统一要求填写方案名称，前端限制最多 64 个字符。
- 后端统一对方案名称执行去除首尾空格、非空和 64 字符长度校验。
- 复制方案时支持输入新方案名称，同时保留未传名称时自动生成副本名称的接口兼容能力。
- 方案详情页 Excel、PNG 和 PDF 导出名称统一为“方案名称 + 当前视角 + 座位表”。
- 导出文件名中的 Windows 非法字符会自动替换，避免浏览器下载失败。

验证记录：

```text
mvn -DskipTests compile
npm run build:prod
```

验证结果：

- 后端编译成功。
- 前端生产构建成功。
- 用户已确认成绩管理真实登录态联调和 Excel、PNG、PDF 导出内容校验完成。
- 本次新增的复制方案命名交互尚未做真实登录态页面操作验证。

## 2026-06-18 MVP 闭环回归环境阻塞记录

已完成：

- 后端执行 `mvn -DskipTests package` 成功，最新 `ruoyi-admin.jar` 已生成。
- 前端开发服务可在 `8222` 正常启动。
- 使用本机 Edge 和 Playwright 完成登录页冒烟检查，页面可正常渲染，无空白页或 Vite 错误覆盖层。
- 实际尝试启动后端并核对启动日志。

当前阻塞：

- 本机 MySQL `3306` 未运行，后端连接 `localhost:3306/intelligent_seating` 时返回 `Connection refused`。
- 本机 Redis `6379` 未运行。
- 因后端无法启动，验证码接口返回 500，完整登录态 MVP 闭环回归无法继续。

环境恢复后继续：

1. 启动 MySQL 和 Redis。
2. 启动后端 `8310` 与前端 `8222`。
3. 验证复制方案时填写新名称、列表回显和详情页标题。
4. 继续执行生成、微调、保存、确认和三类导出的完整闭环回归。

## 2026-06-16 阶段 6 人工微调完成记录

已完成：

- 座位方案详情页支持拖拽已分配学生到空座位或与其他学生交换座位。
- 已锁定座位禁止拖拽调整，目标座位已锁定时阻止交换。
- 座位方案详情页支持锁定和解锁单个学生座位，并在有未保存调整时启用「保存调整」。
- 新增 `PUT /seating/assignment/plan/{planId}`，用于保存方案内人工调整后的座位分配和锁定状态。
- 后端保存人工微调时校验方案归属、座位归属、座位可用性、重复分配和目标座位占用。
- 修复交换两个已有座位时触发 `uk_seat_assignment_seat` 唯一索引冲突的问题：保存时在事务内先临时释放待调整分配，再写回目标座位。
- 保存后分配来源更新为 `MANUAL`，并同步座位行列坐标。

验证记录：

```text
mvn -DskipTests compile
mvn -DskipTests package
npm run build:prod
POST /login
POST /seating/plan
POST /seating/assignment
PUT  /seating/assignment/plan/{planId}
GET  /seating/assignment/list
DELETE /seating/assignment/{assignmentIds}
DELETE /seating/plan/{planId}
```

验证结果：

- 后端编译和打包成功。
- 前端生产构建成功。

## 2026-06-16 座位方案确认记录

已完成：

- 新增 `PUT /seating/plan/{planId}/confirm`，用于将草稿或归档方案确认为正式启用方案。
- 确认方案前校验方案归属和座位分配，空方案不能确认。
- 同一班级确认新方案时，原启用方案自动归档，保证一个班级只有一个当前正式方案。
- 座位方案列表增加「确认方案」按钮和行内「确认」操作。
- 座位方案详情页增加「确认方案」按钮；存在未保存人工调整时，必须先保存调整再确认。

验证记录：

```text
mvn -DskipTests compile
mvn -DskipTests package
npm run build:prod
GET /captchaImage
```

验证结果：

- 后端 Maven 编译成功。
- 后端完整打包成功，并已使用新 jar 启动到 `8310`。
- `http://localhost:8310/captchaImage` 返回 `200`。
- 前端 Vite 生产构建成功。
- 使用真实登录态创建临时手动方案和 2 条临时分配，调用保存接口后，两个学生座位成功互换。
- 验证 `assignmentId=43` 从 `seatId=99` 调整到 `seatId=100`，`isLocked=1`，`assignSource=MANUAL`。
- 验证 `assignmentId=44` 从 `seatId=100` 调整到 `seatId=99`，`assignSource=MANUAL`。
- 验证产生的 `CodexManualAdjustVerify` 临时方案和临时分配已通过业务接口清理，列表剩余数量为 `0`。

当前优先级更新：

1. 补充标记空座、不可信座位或不可用座位的方案编辑能力。
2. 处理人工微调后的规则冲突提示或评分变化提示。
3. 进入座位方案导出和历史方案能力。

## 2026-06-16 人工微调后评分刷新记录

已完成：

- `SeatingEngine` 新增按已有座位分配进行复评估的 `evaluate` 能力，人工微调和自动生成共用同一套硬规则冲突与软规则评分逻辑。
- `PUT /seating/assignment/plan/{planId}` 保存人工微调后，会重新计算方案总分、硬规则冲突和评分明细。
- 保存后替换当前方案的 `seat_plan_score` 评分明细，并更新 `seat_plan.total_score`。
- 保存接口返回 `updated`、`totalScore`、`scoreChange` 和 `conflicts`，供前端展示结果。
- 座位方案详情页保存后显示中文提示：有硬规则冲突时展示警告，无冲突时展示最新总分和分数变化。

验证记录：

```text
mvn -DskipTests compile
mvn -DskipTests package
npm run build:prod
POST /login
POST /seating/plan
POST /seating/assignment
PUT  /seating/assignment/plan/{planId}
GET  /seating/score/list
GET  /seating/plan/{planId}
DELETE /seating/score/{scoreIds}
DELETE /seating/assignment/{assignmentIds}
DELETE /seating/plan/{planId}
```

验证结果：

- 后端编译和打包成功，前端生产构建成功。
- 临时方案保存人工微调后返回 `updated=2`、`totalScore=100.00`、`scoreChange=100.00`、`conflictCount=0`。
- 当前方案评分明细重算后写入 `9` 条记录。
- `seat_plan.total_score` 已同步为接口返回的最新总分。
- 验证产生的 `CodexScoreRefreshVerify` 临时方案、分配和评分明细已通过业务接口清理，列表剩余数量为 `0`。

## 2026-06-16 座位卡性别标识记录

已完成：

- 座位方案详情页加载当前班级学生列表，按 `studentId` 建立学生信息映射。
- 已分配座位的学生姓名前增加性别标签。
- 男生显示蓝色「男」标签，女生显示粉色「女」标签，未知显示灰色「未知」标签。
- 保持座位卡整体状态色不变，避免和已分配、锁定、空座、不可用状态语义冲突。

验证记录：

```text
npm run build:prod
```

验证结果：

- 前端生产构建成功。

## 2026-06-17 方案详情页空座编辑记录

已完成代码实现：

- 座位方案详情页已支持将已分配学生从座位移出，形成方案内空座。
- 详情页右侧新增「未安排学生」区域，展示当前班级中未出现在方案分配里的学生。
- 未安排学生支持拖拽到空座，重新加入当前方案。
- 锁定座位禁止设为空座，未安排学生不能直接拖入已占用座位，避免误覆盖。
- `PUT /seating/assignment/plan/{planId}` 支持保存时删除未提交的旧分配，并支持插入新拖回座位的学生分配。
- 保存后仍复用现有评分刷新逻辑，重新计算方案总分、评分明细和硬规则冲突。

验证记录：

```text
mvn -DskipTests compile
npm run build:prod
```

验证结果：

- 后端 Maven 编译成功。
- 前端 Vite 生产构建成功。
- 本次尚未做真实登录态下的页面拖拽和保存接口联调。

当前优先级更新：

1. 做一次真实登录态下的空座编辑、未安排学生拖回和保存接口联调。
2. 推进座位方案导出能力，优先支持 Excel。
3. 推进历史方案列表、复制和恢复能力。

## 2026-06-17 座位方案 Excel 导出记录

已完成：

- 新增座位表导出行模型 `SeatPlanSeatExportRow`，用于承载 Excel 导出列。
- 后端新增 `POST /seating/plan/{planId}/export-seat-table`，沿用 `seating:plan:export` 权限。
- 导出前复用当前方案权限校验，确保非管理员只能导出自己班级的方案。
- 导出内容按教室布局行列顺序生成，包含方案名称、班级、教室布局、行列、座位编号、座位状态、学生姓名、锁定状态和分配来源。
- 座位状态覆盖已安排、空座、过道、空位和不可用座位。
- 座位方案详情页新增「导出 Excel」按钮，存在未保存调整时禁止导出，避免导出旧数据。

验证记录：

```text
mvn -DskipTests compile
npm run build:prod
```

验证结果：

- 后端 Maven 编译成功。
- 前端 Vite 生产构建成功。
- 本次尚未做真实登录态下的浏览器下载联调。

当前优先级更新：

1. 使用真实登录态验证空座编辑、未安排学生拖回、保存回显和 Excel 下载。
2. 推进历史方案列表、复制和恢复能力。
3. 继续补充 PDF / 图片导出能力。

## 2026-06-17 座位方案 Excel 导出版式修正记录

已完成：

- 修正 `POST /seating/plan/{planId}/export-seat-table` 的导出版式，从清单型明细改为二维座位表网格。
- Excel 顶部展示方案名称、班级、教室布局和总评分。
- 按教室座位行列生成单元格，每个普通座位显示座位编号和学生姓名，锁定座位额外显示「已锁定」。
- 空座、过道、空位和不可用座位在网格中保留位置，并用不同底色区分。
- 根据教室讲台位置展示讲台，支持前、后、左、右四种方向。

验证记录：

```text
mvn -DskipTests compile
```

验证结果：

- 后端 Maven 编译成功。
- 本次尚未做真实登录态下的浏览器下载和 Excel 内容人工核对。

## 2026-06-17 历史方案复制和恢复入口记录

已完成：

- 新增 `POST /seating/plan/{planId}/copy`，用于将已有座位方案复制为新的草稿方案。
- 复制方案时保留原方案的班级、教室布局、总评分、备注、座位分配和评分明细。
- 复制出的新方案状态固定为 `DRAFT`，类型为 `MANUAL`，名称自动追加「副本」和时间戳，避免误覆盖原方案。
- 复制方案名称已按 `seat_plan.plan_name` 长度限制做截断保护，避免长方案名导致保存失败。
- 后端复制过程在事务内完成，座位分配和评分明细重新生成主键，不复用原记录 ID。
- 座位方案列表新增「复制方案」批量工具栏入口和行内「复制」入口。
- 归档方案行内启用入口文案改为「恢复」，复用已有 `PUT /seating/plan/{planId}/confirm` 逻辑，恢复后同班级原启用方案自动归档。

验证记录：

```text
mvn -DskipTests compile
npm run build:prod
```

验证结果：

- 后端 Maven 编译成功。
- 前端 Vite 生产构建成功。
- 已由用户完成验收，复制方案、恢复启用和页面跳转满足当前阶段要求。

当前优先级更新：

1. 继续补充 PDF / 图片导出能力。
2. 梳理演示数据和 MVP 收尾清单。
3. 做一次完整 MVP 闭环验证。

## 2026-06-17 座位方案图片导出记录

已完成：

- 座位方案详情页新增「导出图片」按钮。
- 图片导出复用当前页面已加载的座位布局、学生分配、性别、锁定状态和讲台方向数据。
- 导出前检查是否存在未保存调整，未保存时禁止导出，避免图片内容和后端保存状态不一致。
- 使用浏览器 Canvas 直接生成 PNG 文件，不新增前端依赖，也不新增后端接口。
- 图片顶部展示方案名称、班级、教室布局、总评分和导出时间。
- 图片内按教室二维网格展示普通座位、空座、过道、不可用座位、已锁定座位和学生姓名。

验证记录：

```text
npm run build:prod
```

验证结果：

- 前端 Vite 生产构建成功。
- 本次尚未做真实登录态下的浏览器 PNG 下载和图片内容人工核对。

当前优先级更新：

1. 使用真实登录态核对 Excel 和 PNG 导出文件内容。
2. 继续补充 PDF 导出能力。
3. 梳理演示数据和 MVP 收尾清单。

## 2026-06-17 座位方案 PDF 导出记录

已完成：

- 座位方案详情页新增「导出 PDF」按钮。
- PDF 导出复用图片导出的 Canvas 座位表快照，保证 Excel 之外的可视化导出和页面座位布局一致。
- 导出前同样检查未保存调整，避免导出内容和后端保存状态不一致。
- 点击导出后打开 A4 横向打印页并自动触发浏览器打印，老师可在系统打印对话框中保存为 PDF。
- 打印页保留方案名称、班级、教室布局、总评分、导出时间、讲台方向、座位状态、学生姓名和锁定状态。

验证记录：

```text
npm run build:prod
```

验证结果：

- 前端 Vite 生产构建成功。
- 本次尚未做真实登录态下的浏览器打印保存 PDF 和内容人工核对。

当前优先级更新：

1. 使用真实登录态核对 Excel、PNG 和 PDF 导出文件内容。
2. 梳理演示数据和 MVP 收尾清单。
3. 做一次完整 MVP 闭环验证。

## 2026-06-18 座位方案图片和 PDF 导出修复记录

问题：

- 点击「导出图片」或「导出 PDF」没有反应。
- 前端控制台报错：`Uncaught ReferenceError: parseTime is not defined`。
- 报错发生在 `detail.vue` 的 Canvas 导出头部绘制函数 `drawImageHeader` 中。

已完成：

- 在 `RuoYi-Vue3-master/src/views/seating/plan/detail.vue` 中显式导入若依时间格式化工具 `parseTime`。
- 保持模板展示和 Canvas 导出使用同一个 `parseTime` 实现，避免导出函数访问不到全局属性。

验证记录：

```text
npm run build:prod
```

验证结果：

- 前端 Vite 生产构建成功。
- 本次修复已消除 `parseTime` 未定义导致的打包和运行引用风险。
- 尚未做真实登录态下的浏览器 PNG 下载和 PDF 打印保存人工核对。

## 2026-06-18 座位方案详情页视角切换记录

已完成：

- 座位方案详情页新增「教师视角 / 学生视角」切换控件。
- 默认保持原有展示方向为「教师视角」，避免影响老师当前使用习惯。
- 切换到「学生视角」时，页面座位表按相反视角展示，行列顺序和讲台方向同步反转。
- 视角切换只影响页面展示和 PNG / PDF 快照，不改变座位分配数据、拖拽保存逻辑和后端接口。
- PNG / PDF 导出头部已增加当前视角标识，导出内容跟随当前页面视角。

验证记录：

```text
npm run build:prod
```

验证结果：

- 前端 Vite 生产构建成功。
- 尚未做真实登录态下的页面切换、PNG 下载和 PDF 打印保存人工核对。

## 2026-06-18 座位方案 Excel 视角导出记录

已完成：

- 座位方案详情页导出 Excel 时会提交当前页面 `viewMode`。
- 后端 `POST /seating/plan/{planId}/export-seat-table` 已支持根据 `TEACHER` / `STUDENT` 视角重排二维座位网格。
- 学生视角下，Excel 中的座位行列顺序和讲台方向会与页面、PNG、PDF 保持一致。
- Excel 标题摘要已增加当前导出视角标识，便于老师区分导出版本。

验证记录：

```text
mvn -DskipTests compile
npm run build:prod
```

验证结果：

- 后端 Maven 编译成功。
- 前端 Vite 生产构建成功。
- 尚未做真实登录态下的 Excel 下载和内容人工核对。

## 2026-06-18 成绩管理扩展记录

已完成代码实现：

- 班级管理新增 `school_stage`、`grade_code` 业务字段映射，前端年级从手动输入改为下拉选择。
- 新增成绩扩展 SQL 草案：`RuoYi-Vue-springboot3/sql/seating_score_20260618.sql`。
- 新增 `seat_subject`、`seat_exam`、`seat_student_score` 对应后端 domain、mapper、service 和 controller。
- 成绩管理只绑定班级，考试批次创建时从班级快照学段、年级和科目，不在成绩里重复选择年级。
- 小学默认科目包含语文、数学、英语、科学；初中和高中按学段年级使用不同科目模板。
- 成绩导入支持动态科目列，按学号匹配学生，导入后自动计算总分、排名和 A/B/C/D 成绩等级。
- 新增同步入口，可将指定考试的成绩等级同步回 `seat_student.score_level`，继续服务现有排座算法。
- 前端新增成绩管理页面：`RuoYi-Vue3-master/src/views/seating/score/index.vue`。
- 前端新增成绩相关 API：`src/api/seating/exam.js`、`src/api/seating/studentScore.js`。
- Excel 导入弹窗下载模板时已支持透传额外参数，成绩模板可按班级生成。

验证记录：

```text
mvn -DskipTests compile
npm run build:prod
```

验证结果：

- 后端 Maven 编译通过。
- 前端 Vite 生产构建通过。
- 尚未导入 `seating_score_20260618.sql` 到真实数据库。
- 尚未在真实登录态下联调成绩菜单、考试创建、模板下载、成绩导入和等级同步。

下一步：

1. 确认后导入 `seating_score_20260618.sql`。
2. 刷新菜单权限，检查「成绩管理」菜单是否可见。
3. 用真实班级创建考试批次，下载模板并导入一份小学含「科学」的成绩数据。
4. 验证成绩等级同步后，重新生成座位方案能继续使用学生成绩等级。

## 2026-06-18 MVP 闭环验证记录

已完成：

- 使用真实登录态完成一次完整闭环验证：`登录 -> 新建班级 -> 新建学生 -> 新建教室 -> 保存座位布局 -> 生成方案 -> 人工交换座位并锁定 -> 确认方案 -> 按当前视角导出 Excel -> 清理临时数据`。
- 本次闭环验证中，临时方案生成前后座位分配数量均为 `4`，人工调整后锁定座位数量为 `1`，方案确认后状态为 `ACTIVE`。
- 已验证 `POST /seating/plan/{planId}/export-seat-table` 会随 `viewMode=TEACHER/STUDENT` 输出不同内容：教师视角文件包含“教师视角”，学生视角文件包含“学生视角”，且两个导出文件的工作表 XML 不同。
- 已修复 `seat_class` 查询与当前数据库结构不兼容的问题：`SeatClassMapper.xml` 查询阶段不再直接依赖当前库中不存在的 `school_stage`、`grade_code` 列，班级列表和按名称回查已恢复可用。
- 验证产生的 `Codex*`、`Dbg*`、`ExpDbg*` 临时班级、学生、教室和方案数据已全部清理完成。

发现的兼容风险：

- 当前数据库 `seat_class.semester` 字段长度与现有页面录入习惯不一致，传“上学期”会触发 `Data too long for column 'semester'`。现阶段真实联调需使用短值 `1/2`。
- 当前班级扩展字段 `school_stage`、`grade_code` 代码已进入后端和前端，但数据库还未完成对应结构落库。这次只做了查询兼容，没有做 schema 变更。

验证记录：

```text
mvn -DskipTests compile
POST /login
POST /seating/class
GET  /seating/class/list
POST /seating/student
GET  /seating/student/list
POST /seating/classroom
PUT  /seating/position/classroom/{classroomId}/layout
POST /seating/plan/generate
GET  /seating/assignment/list
PUT  /seating/assignment/plan/{planId}
PUT  /seating/plan/{planId}/confirm
POST /seating/plan/{planId}/export-seat-table?viewMode=TEACHER
POST /seating/plan/{planId}/export-seat-table?viewMode=STUDENT
DELETE /seating/plan/{planId}
DELETE /seating/classroom/{classroomId}
DELETE /seating/student/{studentId}
DELETE /seating/class/{classId}
```

验证结果：

- 后端 Maven 编译成功。
- 真实登录态闭环验证通过，当前“当前视角导出 Excel”链路可用。
- 班级列表恢复可用，但 `semester` 字段长度和 `school_stage` / `grade_code` 未落库仍是 MVP 收尾前必须处理的兼容问题。

下一步优先做：

1. 对齐 `seat_class` 的数据库结构、页面表单和后端字段语义，至少完成 `semester`、`school_stage`、`grade_code` 的一致性收口。
2. 用真实浏览器页面补做教师视角 / 学生视角下的 PNG、PDF 手工导出核对。
3. 在当前闭环已通过的基础上，再决定是否推进成绩管理 SQL 落库和联调。

## 2026-06-18 当前进度同步：成绩管理进入待联调

当前状态：

- 成绩管理扩展的后端、前端、SQL 草案和设计文档已经完成。
- 年级归属已调整为班级维度，成绩管理只选择班级和考试批次。
- 小学科目模板已包含「科学」，初中、高中使用不同学段科目模板。
- 成绩导入后可自动计算总分、排名和 A/B/C/D 成绩等级。
- 成绩等级可同步回学生档案的 `score_level`，继续参与现有排座算法。

已验证：

```text
mvn -DskipTests compile
npm run build:prod
```

待办：

1. 导入 `RuoYi-Vue-springboot3/sql/seating_score_20260618.sql`。
2. 刷新菜单和权限，确认「成绩管理」菜单可见。
3. 创建一个小学班级考试批次，下载模板，确认模板包含「科学」。
4. 导入成绩 Excel，验证排名、等级计算和失败回滚。
5. 同步成绩等级到学生档案，再生成座位方案验证成绩均衡规则仍可使用。

阻塞：

- 真实数据库尚未执行成绩扩展 SQL，因此成绩菜单和接口还不能完成端到端联调。

## 2026-06-18 当前进度同步：成绩扩展 SQL 已导入

当前状态：

- `RuoYi-Vue-springboot3/sql/seating_score_20260618.sql` 已由用户导入数据库。
- 成绩管理相关表、班级年级扩展字段、科目模板和菜单权限已进入可联调状态。
- 代码侧已完成后端编译和前端构建验证。

下一步优先级：

1. 刷新或重新登录后台，确认「智能排座 / 成绩管理」菜单可见。
2. 在班级管理中确认已有班级已补齐年级下拉值；旧班级如仍只有 `grade_name`，需要保存一次以写入 `grade_code` 和 `school_stage`。
3. 在成绩管理中新建考试批次。
4. 下载成绩模板，重点确认小学班级模板包含「科学」列。
5. 导入成绩 Excel，验证总分、排名和 A/B/C/D 等级计算。
6. 执行「同步等级」，确认学生管理中的成绩等级已更新。
7. 重新生成座位方案，确认成绩均衡规则继续按最新等级参与评分。

待验证：

- 成绩菜单真实可见性。
- 成绩模板下载。
- 成绩导入成功与失败回滚。
- 成绩等级同步到 `seat_student.score_level`。
- 同步后排座算法联动效果。

## 2026-06-18 班级管理学期枚举收口记录

已完成：

- `seat_class` 查询、插入和更新已恢复 `school_stage`、`grade_code` 真实字段映射，不再继续使用临时兼容空值。
- `SeatClassServiceImpl` 已补充班级数据归一化：`semester` 统一收口为 `1/2`，并在缺少 `schoolStage`、`gradeName` 时根据 `gradeCode` 自动补齐。
- `RuoYi-Vue3-master/src/views/seating/class/index.vue` 已重建为可维护版本，修复原页面模板乱码导致的属性语法问题。
- 班级页查询区、编辑弹窗和列表展示已统一改为年级下拉、学期下拉和学期中文展示，避免再向后端提交“上学期/下学期”长文本。

验证记录：

```text
mvn -DskipTests compile
npm run build:prod
```

验证结果：

- 后端 Maven 编译成功。
- 前端 Vite 生产构建成功。
- 班级管理页当前已按 `gradeCode + schoolStage + semester(1/2)` 这组业务字段收口。

后续建议：

1. 用真实浏览器新增一个班级并编辑一个旧班级，确认旧数据保存后能补齐 `grade_code` 和 `school_stage`。
2. 再跑一次成绩模板下载，确认班级扩展字段补齐后对成绩模块无回归。

## 2026-06-19 成绩管理新增考试显示修复记录

已完成：

- 修复 `POST /seating/exam` 新增考试成功后只返回通用成功状态、未返回新考试 `examId` 的问题。
- 后端新增考试成功后返回已写入主键的 `SeatExam` 对象，便于前端定位刚创建的考试批次。
- 成绩管理页新增考试成功后会自动切换班级筛选、刷新考试下拉、选中新建考试，并刷新成绩列表。
- 成绩列表空状态已区分「未选择考试」和「当前考试暂无成绩」，避免新增考试后误判为列表未刷新。

验证记录：

```text
mvn -DskipTests compile
npm run build:prod
```

验证结果：

- 后端 Maven 编译成功。
- 前端 Vite 生产构建成功。
- 本次尚未在真实登录态浏览器中复核新增考试后的页面显示和下拉选中效果。

## 2026-06-19 成绩管理考试列表和班级科目修复记录

已完成：

- 新增班级科目配置字段 `seat_class.subject_snapshot`，迁移脚本为 `RuoYi-Vue-springboot3/sql/seating_class_subject_20260619.sql`。
- 班级管理新增和修改弹窗已支持选择班级科目，列表中可查看班级科目。
- 新建考试时优先使用班级科目快照生成考试科目，不再直接依赖年级科目模板。
- 成绩导入模板优先使用班级科目生成表头，科目解析统一去重，避免历史重复科目污染新考试。
- 成绩管理页已调整为先显示考试批次列表，选择考试后再查看和导入该考试成绩。

验证记录：

```text
mvn -DskipTests compile
npm run build:prod
```

验证结果：

- 后端 Maven 编译成功。
- 前端 Vite 生产构建成功。
- 尚未执行数据库迁移脚本，尚未做真实登录态页面复核。

后续必须执行：

1. 执行 `RuoYi-Vue-springboot3/sql/seating_class_subject_20260619.sql`。
2. 用真实页面新增或编辑一个班级，确认科目保存和回显正常。
3. 新增考试，确认考试列表显示、科目不重复且来自班级配置。
4. 下载成绩模板，确认模板表头使用班级科目。

## 2026-06-19 成绩明细独立页面调整记录

已完成：

- 用户已确认 `RuoYi-Vue-springboot3/sql/seating_class_subject_20260619.sql` 已导入数据库。
- 成绩管理主页面已调整为考试批次列表，不再在同页直接展示成绩明细。
- 点击考试行操作区的「查看成绩」会跳转到 `/seating/score-detail/index/{examId}` 独立页面。
- 新增 `RuoYi-Vue3-master/src/views/seating/score/detail.vue`，用于展示考试信息、科目、成绩列表、导入成绩、同步等级、修改和删除成绩。
- 新增隐藏动态路由 `SeatingScoreDetail`，权限沿用 `seating:studentScore:list`，侧边栏高亮仍指向成绩管理。
- 新增考试成功后，列表页会保留对应班级筛选并清空考试名称筛选后刷新，避免旧筛选条件导致新增考试不可见。

验证记录：

```text
mvn -DskipTests compile
npm run build:prod
```

验证结果：

- 后端 Maven 编译成功。
- 前端 Vite 生产构建成功。
- 本次尚未通过真实登录态浏览器复核跳转和导入交互。

## 2026-06-19 学生导入弹窗交互调整记录

已完成：

- 学生管理页的导入流程已从「先选择班级，再下一步上传」调整为一个「学生导入」弹窗。
- 学生导入弹窗内直接展示班级下拉和文件上传区域，班级默认带入当前列表筛选班级。
- 未选择班级时会阻止提交上传并提示「请选择导入班级」。
- 通用 `ExcelImportDialog` 新增顶部插槽和提交前校验钩子，保持成绩导入、用户导入等既有用法不变。

验证记录：

```text
npm run build:prod
```

验证结果：

- 前端 Vite 生产构建成功。
- 本次尚未通过真实登录态浏览器复核学生导入弹窗点击和文件上传交互。

## 2026-06-19 排座规则权重接入修复记录

已完成：

- 修复排座引擎评分固定输出全部评分项、未按启用规则过滤的问题。
- 修复总分固定简单平均、未按 `seat_rule.rule_weight` 加权的问题。
- 修复「男女搭配均衡」等软规则只在评分明细中扣分、未参与生成优化的问题。
- 现在仅启用「男女搭配均衡」且权重为 100 时，方案评分和优化会围绕该规则计算，不再被未启用的视力、身高、成绩、纪律、容量、随机种子等评分项稀释。

验证记录：

```text
mvn -DskipTests compile
```

验证结果：

- 后端 Maven 编译成功。
- 本次尚未通过真实登录态页面重新生成座位方案复核男女搭配效果。

## 2026-06-19 过道布局同桌识别修复记录

已完成：

- 修复「男女搭配均衡」在带过道布局中漏算同性同桌的问题。
- 原同桌判断固定按奇偶列识别，只能识别 `C1-C2`、`C3-C4` 这类连续列，无法正确识别过道后的 `C4-C5`、`C10-C11`。
- 现在同桌判断改为按每行可用座位从左到右两两配对，过道和不可用座位不参与配对。
- 生成方案、手动保存调整后的评分重算、学生关系中的「不能同桌/偏好同桌」已统一使用新的同桌识别逻辑。

验证记录：

```text
mvn -DskipTests compile
```

验证结果：

- 后端 Maven 编译成功。
- 本次尚未通过真实页面重新生成或保存方案复核评分明细。

## 2026-06-19 MVP 真实测试收口记录

已确认：

- 用户已完成成绩管理闭环真实测试：班级科目、新增考试、成绩详情页、模板下载、成绩导入、同步等级和成绩规则联动。
- 用户已完成学生导入弹窗真实测试：班级选择、模板下载、Excel 上传和按学号更新已有学生流程。
- 用户已完成排座规则回归测试：男女搭配、成绩强弱均衡、不能同桌，以及带过道布局的同桌识别。
- 用户已完成导出回归测试：座位方案 Excel、图片、PDF 导出，以及学生和成绩相关导出。
- 新增 `README.md`，补充项目说明、老师使用流程、导入说明和常见问题。

当前状态：

- MVP 核心流程已进入收尾状态。
- 后续优先级从功能开发转为文档完善、演示数据整理、权限菜单复核和体验增强规划。

最终验证：

```text
mvn -DskipTests compile
npm run build:prod
```

验证结果：

- 后端 Maven 编译成功。
- 前端 Vite 生产构建成功。

待完成：

- 提交本次文档收尾记录。

## 2026-06-22 体验增强 P0 与 P1 第一轮实现

当前状态：

- 用户已确认成绩详情页的学号自然排序、总分升降序和导出顺序已使用真实数据校验通过。
- 阶段 9 体验增强持续推进；导入失败反馈和座位编辑效率增强已完成第一轮实现。

已完成：

- 成绩导入失败时保留导入弹窗、已选文件和业务上下文，并将后端返回的错误行逐条展示。
- 成绩导入服务的错误信息改为纯文本换行，不再使用 HTML 换行标签，避免上传组件截断错误详情。
- 座位方案详情新增撤销／重做、批量选择座位、批量锁定／解锁和未安排学生按姓名或学号快速筛选。
- 座位方案详情新增同班历史方案对比，显示学生座位变化；硬规则冲突提示补充当前座位位置。
- 修复座位编辑工具栏在常见桌面宽度下溢出到评分面板的问题。

验证记录：

```text
mvn -DskipTests compile
npm run build:prod
Playwright：模拟成绩导入失败，验证错误行展示和弹窗保留
Playwright：模拟保存调整失败，验证未保存座位状态保留
Playwright：验证批量锁定、撤销、未安排学生筛选和方案差异展示
```

验证结果：

- 后端 Maven 编译成功，前端 Vite 生产构建成功。
- 浏览器模拟接口回归通过；当前未使用真实业务数据执行失败写操作，避免污染演示数据。

补充验证：

- 已使用真实登录态进入座位方案详情页，并在浏览器侧拦截确认方案、Excel 导出、图片导出和 PDF 导出请求／能力为失败场景。
- 确认或导出失败后，方案详情、草稿状态、确认按钮和未保存编辑入口均保持可用；失败请求未发送到后端。

## 2026-06-22 座位方案真实浏览器回归补充记录

已完成：

- 已恢复本地联调环境：MySQL `3306` 可用，后端通过显式指定 Redis `6380` 参数成功启动，`http://localhost:8310/captchaImage` 返回 `200`。
- 已使用真实登录态完成非破坏性浏览器回归：`登录 -> 一键生成并命名 -> 列表搜索 -> 复制并指定名称 -> 详情页拖拽交换 -> 保存调整 -> 学生视角导出 Excel / 图片 / PDF -> 返回列表核对`。
- 本次真实回归生成了草稿方案 `reg-gen-202606220237`，并复制得到草稿方案 `reg-copy-202606220237`。
- 已确认复制方案详情页可见新名称，列表页可按新名称搜索到对应方案，状态仍为“草稿”。
- 已确认拖拽交换后「保存调整」按钮可用，保存成功后按钮恢复禁用；本次交换样例为 `暴昊城 <-> 何梓涵`。
- 已确认学生视角导出命名生效：Excel 下载为 `reg-copy-202606220237-学生视角-座位表.xlsx`，图片下载为 `reg-copy-202606220237-学生视角-座位表.png`，PDF 弹窗标题为 `reg-copy-202606220237-学生视角-座位表`。

验证记录：

```text
http://127.0.0.1:8222/login
Playwright + Edge：真实登录、生成、复制、拖拽保存、导出、列表回显
```

验证结果：

- 登录页、首页、座位方案列表、方案详情均正常渲染，无空白页和前端错误覆盖层。
- 浏览器控制台未出现业务运行错误；仅有 Element Plus 现存 `el-radio` 废弃用法警告，不阻塞本次回归。
- 生成、复制、保存调整与三类导出链路均已通过真实浏览器验证。

当前未执行：

- 未执行「确认启用当前方案」真实成功回归。当前数据库只有一个班级，执行确认会把现有 `ACTIVE` 方案自动归档，属于会改变现有业务状态的操作。
- 本次新增的临时草稿方案 `reg-gen-202606220237`、`reg-copy-202606220237` 保留在本地数据库，未做删除清理。

## 2026-06-22 座位方案确认启用真实回归记录

已完成：

- 在用户确认允许变更当前启用方案后，已对草稿方案 `reg-copy-202606220237` 执行真实浏览器「确认方案」操作。
- 方案详情页确认弹窗提交成功后，页面出现“确认成功”提示，方案状态从“草稿”切换为“启用”，并且详情页不再显示绿色确认按钮。
- 后端接口状态已核对：`planId=27` 已从 `DRAFT` 变为 `ACTIVE`，`activeTime=2026-06-22`；原启用方案 `planId=14` 已自动变为 `ARCHIVED`。
- 列表页浏览器回归已补证：`reg-copy-202606220237` 在方案列表中显示为“启用”，原方案 `2-副本-20260617123557` 显示为“归档”。

验证记录：

```text
Playwright + Edge：座位方案详情点击确认方案
PUT /seating/plan/27/confirm -> 200
GET /seating/plan/list?pageNum=1&pageSize=100
```

验证结果：

- 真实确认启用链路通过。
- 同班级原启用方案自动归档逻辑通过真实浏览器和接口双重验证。
- 浏览器控制台未出现业务错误；仍只有 Element Plus `el-radio` 废弃警告。

当前状态：

- 当前启用方案已切换为 `reg-copy-202606220237`。
- 临时生成方案 `reg-gen-202606220237` 仍保留为草稿，未清理。

## 2026-06-22 座位方案视角切换告警修复

问题：

- 座位方案详情页每次进入都会在浏览器控制台输出 Element Plus `el-radio` 废弃警告。
- 根因是视角切换控件仍使用 `el-radio-button label` 充当选中值，命中了 Element Plus 3 的弃用路径。

已完成：

- 已将座位方案详情页教师／学生视角切换从 `label` 传值改为 `value` 传值。
- 本次仅修改 `RuoYi-Vue3-master/src/views/seating/plan/detail.vue` ，未扩散到无关页面。

验证记录：

```text
npm run build:prod
Playwright + Edge：打开 /seating/plan-detail/index/27 并采集控制台
```

验证结果：

- 前端 Vite 生产构建成功。
- 座位方案详情页仍能正常展示当前启用方案 `reg-copy-202606220237`。
- 页面控制台 `warning` 与 `error` 均为空，本次 `el-radio` 废弃警告已消失。

