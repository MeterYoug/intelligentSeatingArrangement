# 智能排座系统

本项目是面向老师的 PC Web 智能排座管理系统，基于若依前后端分离版二次开发。当前 MVP 覆盖班级、学生、成绩、教室布局、排座规则、座位方案生成、人工微调和导出流程。

## 目录结构

```text
.
├── RuoYi-Vue-springboot3   后端，若依 Spring Boot 3 多模块工程
├── RuoYi-Vue3-master       前端，若依 Vue3 + Vite + Element Plus 工程
├── docs                    设计和进度文档
└── ROADMAP.md              MVP 路线图和真实进度
```

## 本地运行

后端：

```bash
cd RuoYi-Vue-springboot3
mvn -DskipTests compile
```

前端：

```bash
cd RuoYi-Vue3-master
npm run dev
```

当前默认端口：

```text
后端：http://localhost:8310
前端：http://localhost:8222
验证码：http://localhost:8310/captchaImage
前端代理：http://localhost:8222/dev-api/captchaImage
```

## 老师使用流程

1. 班级管理
   - 新增班级，选择学段、年级、学期和班级科目。
   - 班级科目会用于后续考试和成绩导入模板。

2. 学生管理
   - 维护学生基础信息，包括学号、姓名、性别、身高、视力等级、成绩等级、纪律等级和特殊需求。
   - 点击「导入」后，在同一个弹窗内选择班级并上传学生 Excel。

3. 成绩管理
   - 先新增考试批次。
   - 点击「查看成绩」进入考试成绩详情页。
   - 下载成绩模板，填写各科成绩、总分或排名后导入。
   - 执行「同步等级」后，学生管理中的成绩等级会更新，并参与后续排座。

4. 教室布局
   - 维护教室座位、过道和不可用座位。
   - 带过道布局中，同桌按每行可用座位从左到右两两识别。

5. 排座规则
   - 配置班级规则，例如男女搭配均衡、成绩强弱均衡、纪律关注学生分散、近视靠前、高个靠后、指定学生坐前排。
   - 规则权重会参与方案生成、优化和总分计算。

6. 座位方案
   - 选择班级和教室布局生成方案。
   - 在详情页查看评分明细、未安排学生和座位表。
   - 可手动拖拽微调、锁定座位、保存调整、确认方案。
   - 支持导出 Excel、图片和 PDF。

## 导入说明

- 学生导入只允许 `xls`、`xlsx` 文件。
- 学生导入时必须选择班级，可选择按学号更新该班级已有学生。
- 成绩导入模板按考试所属班级的科目生成，避免小学、初中、高中科目混用。
- 成绩导入后建议执行「同步等级」，再重新生成座位方案验证成绩均衡效果。

## 常见问题

### 为什么男女搭配会扣分？

男女搭配按「同桌」计算。每行可用座位从左到右两两配对，过道和不可用座位不参与配对。同桌两人同性时会计入同性同桌数并扣分。

### 规则权重怎么影响总分？

启用的规则才会生成评分明细。总分按启用规则的 `rule_weight` 加权平均，未启用规则不会稀释总分。

### 修改规则后旧方案会自动重算吗？

不会。旧方案保存的评分明细不会自动变化。需要重新生成方案，或在方案详情页保存一次调整触发重算。

### 成绩等级如何参与排座？

成绩导入后执行「同步等级」，系统会把考试成绩等级同步到学生表的成绩等级字段。之后启用「成绩强弱均衡」规则重新生成座位方案即可参与排座。

## 验证命令

后端编译：

```bash
cd RuoYi-Vue-springboot3
mvn -DskipTests compile
```

前端生产构建：

```bash
cd RuoYi-Vue3-master
npm run build:prod
```
